# Smoke-test config for the multi-symbol refactor on the naive baseline.
# References btc_4h_2024_multi.dataset.yaml, which adds an ETHUSDT training
# selector that the per-series matching rule will ignore. Aggregate metrics
# must equal btc_4h_2024.config.yaml within float epsilon.
dataset: btc_4h_2024_multi   # configs/datasets/btc_4h_2024_multi.dataset.yaml
