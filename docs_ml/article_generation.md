# Prompt
- take the title+short description of a articles in `docs_ml/articles_todo.md`
- create a folders `/docs_ml/articles/${index}-${slugified_title}/` for each article
- create a `${index}-${slugified_title}.outline.md` file first
- then generate the full article in `${index}-${slugified_title}.md`, using the outline as a guide and expanding on it with more details, code snippets, and explanations.